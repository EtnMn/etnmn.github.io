---
interface Props {
    text: string;
}

const { text: copyright } = Astro.props;
---

{
    copyright && (
        <p class="p-3 mt-10 text-center text-gray-500 dark:text-gray-400">
            {copyright}
        </p>
    )
}
