{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "./node_modules/.bin/astro dev --host",
            "name": "Astro Development server",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}/front"
        }
    ]
}
